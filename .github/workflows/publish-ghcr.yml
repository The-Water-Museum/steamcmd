name: publish

on:
  release:
    types: [published]

env:
  GHCR_ORG: "The-Water-Museum"
  CONTAINER_NAME: "steamcmd"

jobs:
  publish-hello-docker-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: GHCR Login
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and publish image
        run: |
          docker build . --tag ghcr.io/${{ env.GHCR_ORG }}/${{ env.CONTAINER_NAME }}:${{ github.event.release.tag_name }}
          docker run ghcr.io/${{ env.GHCR_ORG }}/${{ env.CONTAINER_NAME }}:${{ github.event.release.tag_name }}
          docker push ghcr.io/${{ env.GHCR_ORG }}/${{ env.CONTAINER_NAME }}:${{ github.event.release.tag_name }}